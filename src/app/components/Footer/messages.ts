import { defineMessages } from 'react-intl';

export const scope = 'Footer';

export default defineMessages({
  licenseMessage: {
    id: `${scope}.license.message`,
    defaultMessage: `Ant Design ©{year} Created by Ant UED`,
  },
});
